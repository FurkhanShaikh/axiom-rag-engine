global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: axiom-rag-engine
    metrics_path: /metrics
    static_configs:
      - targets:
          - axiom-rag-engine:8000
        labels:
          service: axiom-rag-engine
